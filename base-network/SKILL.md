---
name: Base Network
description: "Base Mainnet and Sepolia network configuration — RPC endpoints, chain IDs, explorer URLs, and wallet setup for Base blockchain development."
metadata:
  homepage: https://base.org
  install: external
---

# Base Network

Provided by Base. Installs outside this repo — see `catalog.json` `install.command`.
